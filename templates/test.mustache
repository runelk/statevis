<div class="store-view">
  <form action="">
    <input id="input-load" name="" type="file" value=""/>
    <fieldset>
      <legend>Relations</legend>
      <table>
        {{#relations}}
          <tr class="triple">
            <td><input type="text" value="{{0}}"></td>
            <td><input type="text" value="{{1}}"></td>
            <td><input type="text" value="{{2}}"></td>
          </tr>
        {{/relations}}
      </table>
    </fieldset>
  </form>
</div>
